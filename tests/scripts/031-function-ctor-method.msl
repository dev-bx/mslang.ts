function Counter(start) {
    this.value = start;
    this.inc = function(by) {
        this.value = this.value + by;
        return this.value;
    };
}
c = new Counter(10);
c.inc(2);
c.inc(3);
return c.inc(4);
